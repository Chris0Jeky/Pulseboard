# Owner decisions — Pulseboard

Agents surface this file in every summary and tick an item only when its completion is directly
verified. Items that need the owner's judgement stay open until the owner answers here or in chat.

- [x] q-1 — **Ratify the tier. DONE 2026-09-10:** owner replied "T2 is good"; tier.json notes now start `Ratified 2026-09-10`. Original item: `.agent-harness/tier.json` declares T2 (daily driver; push free,
  merge free within the global gate). Proposed by the 2026-09-10 harness session on this evidence:
  public repository, no deployment, no accounts or participant data, collection disabled by default,
  durable multi-session use, and a live PR stack. Reply "T2 stands" or name another tier; the agent
  then prepends `Ratified <date>.` to the `notes` field so tier.json alone shows proposed vs ratified.
- [x] q-2 — **Hosted activation gate. AUTHORISED 2026-09-10:** owner replied "go ahead". Execution is issue #19 after the stack lands, and it needs Cloudflare access the executing box does not have (q-4). Original item: Creating any Cloudflare Worker or D1 resource, setting a
  production read token, or turning collection on for any host project requires your explicit
  go-ahead, per `observatory/docs/ROLLOUT.md` and issue #19. Nothing in PRs #15–#17 or issues
  #18–#24 authorises it on its own.
- [x] q-3 — **Confirm the Desk pivot for the merge lane. AUTHORISED 2026-09-10:** owner replied "ship the stack", adding that #15–#17 were prototyping and must be deeply checked and tested first; agents review, fix and merge oldest-first on that basis. Original item: PRs #15 → #16 → #17 replace the root
  README thesis and make `observatory/` the primary product. The pivot is a product decision, so
  T2's `merge: free` does not cover it: no agent merges any of the three until you reply
  "ship the stack" (or name what must change first). Once authorised, the agent review-and-ships
  them oldest first, retargeting each child after its base lands.

- [x] q-4 — **Cloudflare access verified 2026-09-10.** Wrangler 4.130.0 found an existing OAuth login on this machine with Workers and D1 write scopes. The Worker and D1 database are deployed; see `observatory/docs/HOSTING.md`. Scheduled probes were enabled later the same day at the owner's request ("start monitoring some of the published sites"); collection followed for Alibi only under q-7.
- [ ] q-5 — **Trust the reviewed Codex adapter.** Start a fresh Codex session in this checkout, open `/hooks`, and confirm the `.codex/hooks.json` handler is enabled and trusted. Static checks pass but cannot certify this session-only state.
- [x] q-6 — **Account plan and spending notifications. DONE 2026-09-10:** owner replied "done, confirmed free plan". Original item: Confirm the account remains on the intended free plan in Cloudflare's dashboard and select acceptable notifications. The existing OAuth token returns 403 for subscription reads. This deployment makes no paid-plan change. With probes on, the Worker runs 96 cron invocations a day, each with seven outbound fetches and roughly fourteen D1 writes: far inside the free allowances, but the dashboard is the only place that shows the account-wide picture.
- [x] q-7 — **Choose the first collection pilot and review its privacy notice. DECIDED 2026-09-10:** owner replied "pilot Alibi, notice approved". Execution: `COLLECT_ENABLED` is `"true"` on the hosted collector, Alibi#83 carries the artifact with its endpoint and the collector origin in its CSP, and the first consented payload is inspected before any other host is activated (receipts in `observatory/docs/HOSTING.md`). Original item: The collector admits events correctly on hosted D1 (gate passed 2026-09-10, `observatory/docs/HOSTING.md`) and the seven host PRs carry inert, regenerated artifacts (#31). Turning `COLLECT_ENABLED` to `"true"` and giving one host artifact its endpoint is a product and privacy decision, not cleanup: `observatory/docs/ROLLOUT.md` suggests CommitAtlas first (it needs only its notice and CSP review), while MDviewer and Alibi need an explicit notice/release review and Developer Lens needs tests that its private builds never load the integration. Reply with the pilot's name and "notice approved" once you have read that host's notice text; the agent then flips the variable, regenerates that one artifact with `https://pulseboard-observatory.commit-atlas.workers.dev/v1/collect/<id>`, moves the host PR out of draft and inspects the first accepted payload before anything else is activated.
- [x] q-8 — **Confirm GitHub failure notifications reach you. DONE 2026-09-10:** owner replied "done". Original item: `.github/workflows/collector-canary.yml` checks the hosted collector twice an hour from GitHub's runners and goes red on a non-200 `/healthz` or `/readyz` or an open `/v1/portfolio`. GitHub e-mails workflow failures only if "Actions" notifications are on for your account (Settings → Notifications → Actions, "Failed workflows only" is enough). Nothing else pages: a probe reading `down` shows in the Desk and nowhere else.
- [x] q-9: **Map a first repository for GitHub evidence. DECIDED 2026-09-22:** owner replied "map Alibi first, token is fine". Executed: `observatory/src/github-map.mjs` revision 2 maps Alibi by numeric ids (repository 1360756863, `check.yml` workflow 352677495 on `main`, latest 5 releases; no deployment environment because Alibi publishes outside GitHub deployments). The token itself is q-10.
- [x] q-10: **Create and install the GitHub evidence token. DONE 2026-09-23:** owner replied "token set"; the secret is listed on the Worker and the first live reading (`check.yml` passing, five releases observed) is recorded in `observatory/docs/HOSTING.md`. The owner also chose to rotate the read token from this machine. Original item: Follow `observatory/docs/HOSTING.md`, "GitHub evidence token": a fine-grained, read-only token for `Chris0Jeky/Alibi` alone (Actions and Contents read), exported as `GITHUB_EVIDENCE_TOKEN` for the local Desk and, for the hosted Desk, set with `npx wrangler secret put GITHUB_EVIDENCE_TOKEN` after deploying current `main` (the deployed Worker predates the route). Reply "token set" once done; the agent then records the first live reading in `HOSTING.md`.
- [x] q-11: **Admit `puzzle.failed` for the live Alibi pilot? DECIDED 2026-09-23:** owner replied "admit puzzle.failed". Execution: #63 merged and deployed on 2026-09-23 (Worker `eda4e81d…`, admission receipts in `observatory/docs/HOSTING.md`); Alibi's regenerated host artifact and journey hooks merged as Chris0Jeky/Alibi#183 and deployed to the Cloudflare origin the same day (Worker `3d83bc77…`, receipt in Alibi `docs/RELEASE-0.11.5.md`). Follow-ups: #73 here, Chris0Jeky/Alibi#270 and #274. Original item: Draft PR #63 proves the named `puzzle.solve` journey and adds `puzzle.failed` to Alibi's registered events. Alibi is the only project collecting live, so a new admitted event name is a measurement change, and Alibi's host artifact would need regenerating. Reply "admit puzzle.failed" or name what must change; #63 stays a draft until then.
- [x] q-12: **Alibi public Usage sharing default. DEPLOYED 2026-09-25:** owner selected "Public default after redesign." The aggregate-only producer was admitted first, then Alibi 0.12.0 deployed the open notice and persistent opt-out to its primary Cloudflare origin ([release receipt PR](https://github.com/Chris0Jeky/Alibi/pull/367)). A hosted Chromium visit observed one accepted aggregate count and no further send after opt-out. The separate Sites origin and other Pulseboard projects remain outside this default-on path; issue #89 records the scope and remaining jurisdictional uncertainty.

- [x] q-13: **Which sites send usage counts. DECIDED 2026-09-26:** owner selected all public sites: Alibi, CommitAtlas, Portfolio, MDviewer, Developer Lens showcase, IdleHarbor and WealthLens. Rollout order and per-host conditions: `observatory/docs/USAGE_PLAN.md`, slice 4.
- [x] q-14: **What to learn about visitors. DECIDED 2026-09-26:** owner wants "as much as possible" about how testers use the products, for product improvement and security, not marketing, and not to identify individuals. It covers country (coarse), device class, source category and new vs returning, plus an unobtrusive notice (a beta tester complained the current one got in the way). Executed as the two-tier design in `USAGE_PLAN.md`: visitor counts are default-on with a one-line notice; tester journeys need enrolment.
- [x] q-15: **Build or borrow. DECIDED 2026-09-26:** "both, doing the same things, ambitiously", without vendor lock-in. Pulseboard stays the system of record, and Cloudflare Web Analytics runs in parallel behind a replaceable read-only adapter (`USAGE_PLAN.md`).
- [x] q-16: **Legacy workbench. DECIDED 2026-09-26:** the owner delegated the choice and asked to reuse existing work. The agent chose to freeze it and reuse its feed pattern as Desk adapters (#24). No deletion before one adapter works (`USAGE_PLAN.md`).
- [ ] q-17: **Enable Cloudflare Web Analytics and create a read token. Wait until slice 3 (#104) has shipped the opt-out,** so no beacon runs before visitors can turn it off. Cloudflare dashboard → Analytics & Logs → Web Analytics: add each site in **manual (JS snippet) mode only**. Never choose automatic setup: it injects the beacon at the edge, outside the opt-out and GPC/DNT checks. Note each site token. The agent wires the snippet into the embed behind the same opt-out, so paste the site tokens in chat or a PR (they are public beacon tokens, not secrets). Then create an API token with only **Account → Account Analytics → Read** and store it with `npx wrangler secret put CF_ANALYTICS_TOKEN` from `observatory/`. Reply "analytics on" when done. This gates `USAGE_PLAN.md` slice 6.
- [ ] q-18: **Refresh this machine's read-token copy.** `%LOCALAPPDATA%/Pulseboard/read-token.dpapi` on Kraspyon dates from 2026-09-10 and no longer authenticates, because the token was rotated on 2026-09-23 from DESKTOP-IHKOOJS. Either run the `HOSTING.md` save step yourself on Kraspyon, pasting the current token at a secure prompt (a DPAPI file cannot be copied between machines), or say "rotate from Kraspyon" and the agent rotates it here. Rotating logs out copies on other machines. Until then an agent on this machine cannot verify hosted reads; you can still paste your token into the live Desk's **Connect data**.

- [ ] q-19: **Approve the one-line notice wording.** Before slice 3 (#104) ships the new embed, read the proposed text in `observatory/docs/USAGE_PLAN.md` ("Notice: present, not in the way") and reply "notice text approved" or give your wording. The design, one line with no modal, is agreed. The exact words go live on every site, so they need your approval as q-7 and q-12 did.

The encrypted operator read token is stored at `%LOCALAPPDATA%/Pulseboard/read-token.dpapi` for the Windows user on DESKTOP-IHKOOJS; Kraspyon's copy is stale (q-18). `observatory/docs/HOSTING.md` explains how to copy it without printing it. The public demo needs no token.

Tracked debt that needs no owner input: #43 (the first unattended probe cron tick, pending behind Cloudflare
incident sjs8s0q2x4hw, "Workers Cron Triggers degraded", open since 2026-09-09; until it lands every probe
reads `unknown` or `stale` in the Desk), #19 (the remaining query-budget and rollback evidence), #32 (SDK
delivery follow-ups from the Alibi pilot) and the post-merge issues on the Desk delivery order (#18, #20–#24,
#35). #13, #14 and #33 closed on 2026-09-22.
