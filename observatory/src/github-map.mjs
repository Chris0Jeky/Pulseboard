/** Reviewed GitHub mapping, pulseboard.github-map/1. Numeric repository and workflow ids only: names are checked, never matched.
 *  Empty on purpose. Adding a project (Alibi first, with verified ids) is an owner decision (HUMAN_TODO.md q-9), not cleanup. */
export const githubMap = { schema: 'pulseboard.github-map/1', revision: 1, reviewedAt: '2026-09-22', projects: {} };
